已经做好并合并到 `9月新房间判定算法`。

你本地拉最新代码后，跑一次“诊断回放”即可验证。每个人工 ENTER/EXIT 点经过约 350ms 后，顶部原来的算法进出提示仍保留；它下面会额外出现一条青色/深青底的：

`【推断进出】#7 客厅 → 儿童房 ｜ Pose#3 ｜ 几何重叠=0.78`

EXIT 则反向显示 `儿童房 → 客厅`。首个入户事件如果标的是 EXIT，应该显示 `入户 → 客厅`。

这条【推断进出】完全不使用 V4 候选门、锁门、FSM、Ledger 或最终房间结果，只用人工标记时间 ±350ms 内的原始 YOLO Pose 人体框，与所有已标定 Portal 多边形计算真实交叠面积，再取最高者。因此这轮就是专门验证“人工点能不能自动还原正确房门”。

诊断 JSON 也会为每个事件多写 `inferredPortal`：门名、roomId、poseId、重叠分数、人体覆盖率、Portal覆盖率和相对时间。目前我还没有把这个推断门当作硬GT去改 MATCH/WRONG_PORTAL；等你这一次肉眼确认门名都对，再把它正式升级成真值。

按你的要求，这次没有跑云端 Gradle/Android 编译；只执行了源码补丁和静态标记核对。你本地编译后直接跑一遍，看青色【推断进出】是不是每个点都对应正确的门。

任务简报：任务目的=用人工事件时间点自动推断真实房门并可视化核对；修改文件=`MarkedPortalTruthInference.kt`、`EventDiagnosticRecorder.kt`、`MainActivity.kt`、`DetectionOverlayView.kt`、`codexHistory.md`、`dialogueHistory.md`及循环复用对话临时文件；涉及方法=原始Pose人体框与Portal多边形精确交叠面积、±350ms事件窗口取最佳重叠、独立推断消息总线、青色二级提示条、诊断JSON写入inferredPortal。