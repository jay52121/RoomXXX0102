## [406] 2026-09-16 23:27:00 - V4.1 调试层改为逐像素真实 Mask

**用户指令**：
> 好吧，开关门、开关灯，肯定都是一个单独的处理。我们先不说了先把。人体的绘制好好重绘一下，然后我来看到底视觉上是否符合

**实现方案**：

* **只改调试视觉**：不修改门开关、灯光处理、进出判定或人数账本；当前任务只把可视化从轮廓填充改为真实二值像素。
* **三层真实 Mask**：黄色显示相邻帧阈值后的 MotionMask；橙色显示当前帧相对事件固定参考图、形态学处理前的原始背景差；青色显示当前 HumanOwnedMask；B 版绿色光流线继续保留。
* **禁止轮廓填充**：删除 `findContours(RETR_EXTERNAL)` + `Path.fill` 调试路径；新增 `GateMaskDebug`，把每行连续非零像素压缩成水平运行段，零像素洞原样保留，Canvas 关闭抗锯齿后逐段画回源画面坐标。
* **避免陈旧画面**：ARMED/OFF 不做像素处理时清理旧门 mask；视觉异常、几何重建和 close 时同步清理调试缓存。
* **调试面板**：新增动态/背景差/人体三类像素计数和颜色图例。
* **验证**：完整 `:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过；新增 2 条 GateMaskDebug 单测，验证零像素空洞不会被填充、空 mask 不产生绘制段。Debug APK `128269700` bytes，SHA256 `b20d4b3c13a6bd30443669480c8af265b689579c88277445ae6e497a31ecba69`。
* **边界**：如果新 APK 中肉眼不变的区域仍出现黄色/橙色，这时已不是轮廓绘制造成，而是该像素真实超过当前阈值（默认 18）；后续再据此调整阈值/噪声模型。开关门、开关灯本轮明确未处理。

---
