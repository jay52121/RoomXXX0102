项目规则

1) 每次文件级改动后输出任务简报，格式固定为：任务简报：任务目的=...；修改文件=...；涉及方法=...
2) 维护 codexHistory.md：仅当指令导致代码改动时追加；包含序号和时间；“结果”必须与任务简报完全一致（逐字一致）；全部中文、UTF-8。
3) 所有思考与输出必须为中文。
4) 改代码前先给出简要方案并等待确认。
5) 禁止用 shell 重定向写日志；只用编辑文件方式（apply_patch）修改日志与规则文件。
6) 添加调试日志无需另行确认。
