# TODO

维护格式：
- 序号：`TODO-XXX`
- 添加时间：优先记录精确时间；若为后补录，明确标注“补录，原始精确时间缺失”
- 状态：待实现 / 进行中 / 已完成 / 已取消

## [TODO-007] 左侧空白区增加放大手预览按钮
- 添加时间：2026-03-31 00:00:00
- 状态：待实现

- 需求摘要：
  - 在左侧空白区域增加一个“放大手”按钮。
  - 点击后，屏幕上原有其它绘制内容暂时不再显示，只保留手部放大预览。
  - 放大内容基于当前 hand ROI 复制一份显示，按 4 倍放大后居中展示。
  - 该功能不能影响任何现有识别流程、ROI 计算流程和播放流程，只允许作为显示层预览。

- 技术方案：
  - 不改 `TextureView` 和视频播放层，不做真实播放缩放。
  - 只在 overlay/显示层新增一个“手部放大预览态”。
  - 预览源直接使用当前 hand ROI，对当前显示帧做局部裁剪后再居中放大绘制。
  - 开启预览时隐藏原有普通 overlay 元素；关闭后恢复原样。
  - 识别链、ROI 链、设备判定链继续使用原始画面与原始 ROI，不接入放大结果。
  - 若当前没有有效 hand ROI，则显示空白或“未检测到手部区域”，不做错误放大。

## [TODO-001] ROI 裁剪框自增自减策略
- 添加时间：2026-03-27（补录，原始精确时间缺失）
- 状态：已完成

- 需求摘要：
  - 当前 `640` 直接裁剪逻辑需要支持按人物框大小自增自减。
  - 当人物框已经达到或超过裁剪框大小时，裁剪框边长增大 `50` 像素，始终保持正方形。
  - 绿框不能贴近裁剪边界；裁剪框最大只能到视频短边。
  - 当人物检测框变小，且长边小于“裁剪框边长 - 50”时，裁剪框缩小 `50`。
  - 裁剪框最小下限保持 `640`。

## [TODO-002] 非 lock 且肩膀不可信目标使用虚线框
- 添加时间：2026-03-27（补录，原始精确时间缺失）
- 状态：待实现

- 需求摘要：
  - 如果某个 ID 当前没有被 `lock`，
  - 且其肩膀高度可信条件也不成立，
  - 那么该人物框改为虚线绘制，
  - 以便和正常可追踪/高可信目标区分。

## [TODO-003] 快速新增房间功能
- 添加时间：2026-03-27 17:15:15
- 状态：待实现

- 需求摘要：
  - 第一次进入次房间设置时，直接弹出一个可多选的预置房间菜单。
  - 候选项包括：主卧、次卧、儿童房、卫生间一、卫生间二、书房、入户门、厨房、盲区。
  - 该功能只能在客厅已经创建完成后启用。
  - 用户勾选确认后，系统在客厅内部平均分布一批初始点。
  - 用户拖动对应点靠近房门/墙边时，对应目标实时高亮。
  - 用户松手后自动绑定房门，并提示“房门绑定成功”。

- 备注：
  - 后续实现时可拆成四块：首次进入触发、多选预置房间名、客厅内自动布点、拖拽绑定房门与高亮反馈。

## [TODO-004] 设备指向链路录制与标记验证
- 添加时间：2026-03-28 04:08:00
- 状态：待实现

- 需求摘要：
  - 当前这一部分开发基本完成，下一阶段主线转为调试验证。
  - 需要重新录制一段已摆放好各种设备的视频，作为统一测试素材。
  - 基于这段新视频进行设备命中事件标记。
  - 结合标记结果反复测试当前设备指向链路。
  - 最终确认整条链路是否能够稳定通过。

- 备注：
  - 该待办以“录制新视频 -> 标记事件 -> 回放测试 -> 问题回修”为主流程推进。

## [TODO-005] 多人同时出现时的指向手归属判定
- 添加时间：2026-03-30 05:18:38
- 状态：待实现

- 需求摘要：
  - 当前在同一时间识别到多个人的手时，
  - “应该以哪个人的手作为指向手”这条判定链仍然存在问题。
  - 需要重新梳理多人场景下的手部归属规则，
  - 明确指向手与目标人物之间的绑定关系，
  - 避免多人同时出现时误选到错误的人手。

## [TODO-006] Firebase 分发接入
- 添加时间：2026-03-30 05:18:38
- 状态：待实现

- 需求摘要：
  - 需要补齐 Firebase 分发相关能力。
  - 后续需明确分发流程、渠道配置、版本产物输出方式以及测试安装链路。
  - 目标是支持后续版本通过 Firebase 进行统一分发与验证。
